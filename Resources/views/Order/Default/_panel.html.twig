<a class="panel" href="{{ path('Order_show', { 'id': order.numberForHumans }) }}">
    <span class="inner">
        <span class="number">
            <span class="label">Order</span>
            <span class="value">{{ order.numberForHumans }}</span>
        </span><!-- .number -->
        <span class="details">
            <span class="buyer">{{ order.buyer.name }}</span>
            <span class="hub">{{ order.hub.name }}</span>
        </span><!-- .details -->
        <div class="order_total">${{ order.total }}</div>
        <span class="clear"></span>
    </span><!-- .inner -->
</a><!-- .panel -->
