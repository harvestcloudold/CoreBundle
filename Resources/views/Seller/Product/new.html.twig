{% extends 'HarvestCloudCoreBundle:Seller:layout.html.twig' %}
{% form_theme form 'HarvestCloudCoreBundle:Form:fields.html.twig' %}

{% block seller_content %}
    <h1>New Product</h1>
    <form action="{{ path('Seller_product_new') }}" method="post" {{ form_enctype(form) }}>
        {{ form_widget(form) }}
        <div class="form row">
            <input type="submit" class="submit button" value="Save" />
        </div><!-- .form.row -->
    </form>
{% endblock %}
